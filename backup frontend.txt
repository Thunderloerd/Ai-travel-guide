<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>AI Travel Guide</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Basic styling -->
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f3f4f6;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 800px;
      margin: 20px auto;
      background: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    h1 {
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }
    label {
      font-weight: bold;
      margin-top: 10px;
      display: block;
    }
    input, textarea, button {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 15px;
    }
    button {
      background: #2563eb;
      color: white;
      cursor: pointer;
      margin-top: 12px;
      border: none;
      font-size: 16px;
    }
    button:hover {
      background: #1e4fcc;
    }
    #loading {
      display: none;
      text-align: center;
      margin-top: 10px;
      font-weight: bold;
      color: #555;
    }
    #results {
      display: none;
      margin-top: 20px;
      padding: 15px;
      background: #f9fafb;
      border-radius: 10px;
      border: 1px solid #e5e7eb;
    }
    .section-box {
      padding: 15px;
      margin-top: 20px;
      background: white;
      border-radius: 10px;
      border: 1px solid #ddd;
    }
    .section-box h2 {
      margin-top: 0;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>AI Travel Guide</h1>

    <!-- INPUT FORM -->
    <form id="travel-form">
      <label for="destination">Destination(s)</label>
      <input type="text" id="destination" placeholder="e.g., Delhi ? Agra ? Jaipur" required />

      <label for="budget">Budget</label>
      <input type="text" id="budget" placeholder="e.g., 30000 INR" />

      <label for="preferences">Preferences</label>
      <input type="text" id="preferences" placeholder="Adventure, Luxury, Food..." />

      <label for="days">Number of Days</label>
      <input type="number" id="days" min="1" placeholder="e.g., 5" required />

      <button type="submit">Generate Itinerary</button>
    </form>

    <!-- LOADING -->
    <div id="loading">Generating itinerary...</div>

    <!-- ITINERARY OUTPUT -->
    <div id="results">
      <h2>Your Personalized Itinerary</h2>
      <div id="itinerary"></div>
    </div>

    <!-- TRANSPORT & HOTELS BUTTONS -->
    <button id="show-flights">Show Flights</button>
    <div id="flights" class="section-box"></div>

    <button id="show-trains">Show Trains</button>
    <div id="trains" class="section-box"></div>

    <button id="show-buses">Show Buses</button>
    <div id="buses" class="section-box"></div>

    <button id="show-hotels">Show Hotels</button>
    <div id="hotels" class="section-box"></div>
<h2 class="section-title">Ask Travel Assistant</h2>
<div class="output-section" id="chat-box" style="height:auto; min-height:60px;"></div>

<input type="text" id="chat-input" placeholder="Ask anything... e.g. Is Manali safe for solo travel?">
<button id="chat-send" type="button">Ask</button>

  </div>

  <!-- MAIN SCRIPT -->
  <script src="script.js"></script>
</body>
</html>
